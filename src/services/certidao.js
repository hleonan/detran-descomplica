export async function emitirCertidaoStub({ cpf, cnh }) {
  // Base pronta para plugar Playwright depois
  // Aqui retornamos “em construção” para não quebrar o front
  return {
    sucesso: false,
    erro: 'Em construção: emissão automática da certidão ainda não implementada no backend'
  };
}
